# Hydronium DOM

`moonstone/hydronium-dom` provides Hydronium's browser DOM host, client runtime, SSR
renderer, and Meteorite adapter.

```sh
moon add moonstone/hydronium-dom
```

The package installs the `hydronium_dom` Lua namespace and resolves its
`hydronium` core dependency automatically.
